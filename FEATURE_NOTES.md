1. In the new scenario screen after adding a name, have a button next to it which generates the
description from a LLM call with the system prompt, generate description noting who what where when why from the 
name that is provided for the scenario.

2. In the new indicator screen need to update the categories available as a drop down.
